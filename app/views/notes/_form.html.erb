<%= form_for @note, html: {class: 'form'} do |f| %>
  <%= f.label 'title' %>
  <%= f.text_field :title %>

  <%= f.label 'description' %>
  <%= f.text_area :description %>

  <%= f.submit 'Save Note', class:"wide-button save-button" %>
<% end %>
